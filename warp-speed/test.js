

var namestring = " a, b, cat, d, e, f, g, h, i, j, k, l, m, n, o, p"
console.log(exampleArray)

console.log(exampleArray.toString() )
console.log(namestring)

var nameArray = []


nameArray.forEach(function(element, itsINdex, theOriginalArray){

	console.log(theElement)
}


nameArray[3]

var listItem = document.createElement ("li");

listItem.textContent = "hello"

var container = document.querySelector("output ul")
container.appendChild (listItem)



nameArray.forEach(make_a_list);

function make_a_list(ei, ix) {
var newnumber= ix+ 4 + "index"
var output = el+newnumber
console.log(output)

container.appendChild(listItem)
}

somethingFancy ("pleather")
somethingFancy ("glitter")
somethingFancy ("velour")
somethingFancy (["one", 2, "nine"])

function somethingFancy (aFancyThing) {
	console.log ("fanciness")
}

addFive(6)

function doMath (incomingNumber) {
	var finalNumber = incomingNumber + 5
console.log(finalNumber)
}


function addCat(incomingString) {
	var finalText = incomingString + "cat"
	console.log(finalText)
}


names.forEach(changebackground)
function changeBackground (el) {
	el.style.backgroundColor = rgb (30, 230, 30, 4)
}



document.querySelectorAll(".output ul li");
